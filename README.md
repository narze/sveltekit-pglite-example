# SvelteKit + PGLite Example

Showcase of using PGLite with SvelteKit with IndexedDB on browser.

## Setup

```bash
pnpm install
pnpm run dev
```

## Updating database schema

1. Modify [schema.ts](./src/lib/db/schema.ts)
2. `pnpm run db:generate`
